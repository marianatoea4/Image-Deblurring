function [X, iter_vec, diff_vec] = projected_gradient_descent(D, Y, max_iter, alpha, epsilon)

    mn = length(Y);
    X = rand(mn, 1);

    for k = 1 : max_iter
        gradient = 2 * D' * (D * X - Y);
        X = X - alpha * gradient;
        X = max(0, min(1, X));
    end
    X_nou = X;
end